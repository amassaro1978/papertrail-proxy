# PaperTrail Copilot Proxy Server
# Copy this to .env and fill in your values

# Required: Your Anthropic API key (never shared with clients)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Server port (default 3334)
PORT=3334

# Optional: Rate limit per device (requests per minute, default 10)
RATE_LIMIT_PER_MIN=10

# Optional: Free tier monthly action limit (default 10)
FREE_TIER_LIMIT=10
